server:
  bind_address:
  mode:

database:
  host:
  port:
  user:
  password:
  dbname:

jwt:
  secret:

mq:
  url:
  queues:
    - name: review-file-diffs
      durable: true
      auto_delete: false
      exclusive: false
      no_wait: false
      args:
